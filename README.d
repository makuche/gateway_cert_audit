"""
# Tasks
- Implement proper error handling if `OpenSSL` function `load_certificate` does not work 
- Set up .env files containing test data, measure if multiprocessing here might be overkill, considering about 200 env files
